Router Directory
================

Includes:
- L1 keyword/fingerprint routing (fast heuristic)
- L2 semantic intent classifier (shallow encoder)
- ShepherdEncoder generating semantic query vectors

Router selects the bestselects best expert adapter using WeightIndex.
