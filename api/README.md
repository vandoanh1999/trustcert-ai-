API Directory
=============

Provides FastAPI interface for:
- /dispatch      → route + retrieve adapter candidates + HMS synthesis
- /merge         → run merging algorithms directly (linear, fisher, etc.)

Used for external integration or front-end UI.
